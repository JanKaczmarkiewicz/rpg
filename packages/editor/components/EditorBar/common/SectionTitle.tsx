import { Typography } from '@material-ui/core';
import { FunctionComponent } from 'react';

const SectionTitle: FunctionComponent = ({ children }) => <Typography variant="h5">{children}</Typography>;

export default SectionTitle;
